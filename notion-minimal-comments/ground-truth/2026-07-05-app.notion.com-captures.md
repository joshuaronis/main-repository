# Ground-truth captures — 2026-07-05, app.notion.com

Captured live from the user's logged-in Notion (English UI, dark mode).
**Domain finding:** the Notion web app now lives at `https://app.notion.com`
(page URLs like `/p/<workspace>/<Title>-<32hex>`), NOT `www.notion.so`.
Manifest matches both. Page id appears in the pathname (32 hex, dashless) —
pathname-only extraction confirmed valid.

Classes are hashed atomic CSS (`x87ps6o …`) except a few semantic survivors:
`.notion-page-content`, `.notion-overlay-container`, `.notion-margin-discussion-item`,
`discussion-level-1` / `discussion-id-<uuid>` spans, `commentFilled` /
`commentFilledSmall` svg classes. Locators key on roles/aria/text + these.

## A. Topbar buttons (standalone page)
```
#0 aria="Lock sidebar open"
#1 aria="" txt="<breadcrumb page title>"
#2 aria="Share"
#3 aria="Copy link"
#4 aria="Page info"
#5 aria="Comments"
#6 aria="Favorite"
#7 aria="Actions"        ← the ••• button. PRIMARY LOCATOR: [aria-label="Actions"]
```

## B. ••• menu (captured on a DATABASE page — "Compact Sets…")
- Menu items are `role="option"` (NOT menuitem; `menuitem` is the sidebar page list).
- Items seen: font pickers (AgDefault/AgSerif/AgMono), Copy link, Copy page
  contents, Duplicate, Move to, Archive, Move to Trash, Present, Small text,
  Full width, **Customize layout** (database back-out signal — no "Customize
  page" on database pages), Use with AI, Suggest edits, Translate, Import,
  Export, Updates & analytics, Version history, Notify me, Connections,
  Open in Mac app.
- TRAP: on database pages "Customize layout" ALSO exists as a page-header
  `role="button"` outside any menu (next to Add cover / Add verification /
  View details). The step-4 scan must be scoped to the overlay container.
- On STANDALONE pages the menu shows **Customize page** (user screenshot) →
  panel with Backlinks / Page discussions / Table of contents / Inline comments.

## C. Inline comments row (standalone page, Customize page panel open)
Row structure: `<div class="x…">` containing
- svg `class="commentFilled …"` (icon)
- `<div role="presentation">Inline comments</div>`   ← label anchor
- `<div role="button" tabindex="0" aria-expanded aria-haspopup="dialog">Default</div>`
  ← VALUE CONTROL; its text is the current value.
Chooser options when opened: `role="menuitem"` "Default" and `role="menuitem"`
"Minimal" (plus the value control itself matching /^default$/). aria-selected
not used (null); selection shown by a checkmark glyph.

## Overlay/menu structure
- `.notion-overlay-container` EXISTS on app.notion.com (1 overlay-ish div,
  2 children at rest). Shield selector valid.
- With ••• open, visible popups: `div[role="menu"]`, `div[role="dialog"]`,
  `div[role="listbox"]` (nested; items' ancestor is the listbox), classes hashed.
- TRAP: the left sidebar has an ALWAYS-VISIBLE `role="menu"` ("Notion AI /
  New agent / page list…") OUTSIDE the overlay container. Baseline with
  nothing open: visible menus inside container = 0 of 1 total. So
  "overlay open" = visible menu/dialog/listbox INSIDE .notion-overlay-container.

## F. Mode signal hunt → NONE FOUND
Attribute dump inside .notion-page-content is essentially identical in both
modes. No comment-position-independent mode signal exists. Long-virtualized-
page limitation (§9.10) accepted: all comments off-screen ⇒ detector sees
NONE ⇒ no action.

## D/E. Detector (document-wide diff, page "…Covariance…", 1 inline comment)
DEFAULT mode:
```
<div cls="notion-margin-discussion-item"> 308x74 desc=41 txt="Joshua Ronis6hBOKM"
<div aria="Comment actions"> 76x28 desc=13
(no "N comment" count icon)
```
MINIMAL mode:
```
<div role="button" aria-label="1 comment"> 40x24 desc=4, contains svg.commentFilledSmall
(no notion-margin-discussion-item)
```
→ DETECTOR:
- DEFAULT_VISIBLE ⇐ `.notion-margin-discussion-item` present (lives OUTSIDE
  .notion-page-content — margin column; search document-wide).
- MINIMAL_ONLY  ⇐ role=button with aria-label /^\d+ comments?$/ (or
  svg.commentFilledSmall) present.
Common to both modes (NOT discriminators): `discussion-level-1` /
`discussion-id-…` spans (the highlighted text), topbar aria="Comments",
block-hover aria="Comment".

## G. Inline vs page-level comments
Not yet captured on a page-discussion-only or resolved-comment page.
`.notion-margin-discussion-item` is margin-specific (inline anchors), so page
discussions (in-flow under the title) should not match — verify during
acceptance test 3/4 era; if a page-discussion-only page ever triggers a run,
revisit this capture.

## I. Sanity
- `.notion-page-content` exists in top document: true. Top frame: true.
